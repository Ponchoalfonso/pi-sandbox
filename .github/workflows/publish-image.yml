name: Publish Docker image

on:
  push:
    branches: ["main"]
    paths:
      - "Dockerfile"
  schedule:
    # Poll daily for new upstream pi releases.
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  attestations: write
  id-token: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ponchoalfonso/pi-sandbox
  UPSTREAM_REPO: earendil-works/pi

concurrency:
  group: publish-docker-image
  cancel-in-progress: false

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Get latest upstream pi release tag
        id: upstream
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="$(gh release view --repo "$UPSTREAM_REPO" --json tagName --jq .tagName)"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "Latest upstream release: $tag"

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Decide whether to build
        id: build-check
        run: |
          image="${REGISTRY}/${IMAGE_NAME}:${{ steps.upstream.outputs.tag }}"

          if [ "${{ github.event_name }}" != "schedule" ]; then
            echo "should_build=true" >> "$GITHUB_OUTPUT"
            echo "Building because this workflow was triggered by ${{ github.event_name }}."
            exit 0
          fi

          if docker manifest inspect "$image" >/dev/null 2>&1; then
            echo "should_build=false" >> "$GITHUB_OUTPUT"
            echo "Skipping build: upstream release ${{ steps.upstream.outputs.tag }} already exists at $image."
          else
            echo "should_build=true" >> "$GITHUB_OUTPUT"
            echo "Building: detected new upstream release ${{ steps.upstream.outputs.tag }}."
          fi

      - name: Extract metadata for Docker
        id: meta
        if: steps.build-check.outputs.should_build == 'true'
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=raw,value=${{ steps.upstream.outputs.tag }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.version=${{ steps.upstream.outputs.tag }}

      - name: Build and push Docker image
        id: push
        if: steps.build-check.outputs.should_build == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Generate artifact attestation
        if: steps.build-check.outputs.should_build == 'true'
        uses: actions/attest@v4
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Delete old package versions
        if: steps.build-check.outputs.should_build == 'true'
        uses: actions/delete-package-versions@v5
        with:
          package-name: ${{ github.event.repository.name }}
          package-type: container
          min-versions-to-keep: 3
